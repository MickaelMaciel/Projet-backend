{# On indique de quel template on hérite.
   Le chemin est à indiquer depuis le répertoire racine des templates (templates/) #}
{% extends "base.html.twig" %}

{# On déclare les blocs dont on souhaite écraser le contenu.
   Les autres blocs garderont le contenu indiqué dans le template parent #}
{% block body %}

    {# Menu qui sera affiché sur toutes les pages du Front-Office #}
    <nav class="navbar has-background-grey-darker is-dark">
        <div class="navbar-brand">
            <a class="navbar-item" href="{{ path('home') }}">  
                <p class="subtitle is-4 has-text-primary">
                    <i class="fab fa-cuttlefish"></i></i><b>REATEXTE</b>
                </p>
            </a>

            <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
            </a>
        </div>

        <div id="navbarBasicExample" class="navbar-menu">
            <div class="navbar-start">
            
                {# <a class="navbar-item" href="">
                    CREER
                </a>

                <a class="navbar-item" href="">
                    MODIFIER
                </a>

                <a class="navbar-item" href="">
                    CONSULTER
                </a>  #}
               
                <div class="navbar-item has-dropdown is-hoverable">
                    <a class="navbar-link">PRODUIT</a>

                        <div class="navbar-dropdown">
                            
                            <a class="navbar-item" href="{{ path('typeProduit_list') }}">
                                TYPE PRODUIT
                            </a>

                            <a class="navbar-item" href="{{ path('sousTypeProduit_list') }}">
                                SOUS TYPE PRODUIT
                            </a>

                            <a class="navbar-item" href="{{ path('produitConfigurable_list') }}">
                                CONFIGURABLE
                            </a>

                            <a class="navbar-item" href="{{ path('articleSimple_list') }}">
                                ARTICLE SIMPLE
                            </a>

          
                            {# <hr class="navbar-divider"> #}
                              
                        </div>
                </div>
    
                <a class="navbar-item" href="{{ path('fabricant_list') }}">
                    FABRICANT
                </a>

                <a class="navbar-item" href="{{ path('variationCouleur_list') }}">
                    COULEUR
                </a>

                <a class="navbar-item" href="{{ path('image_list') }}">
                    IMAGE
                </a>


                

              

                  {% if is_granted('ROLE_EDITOR') %}
                    
                 <a class="navbar-item" href="{{ path('user_list') }}">
                    UTILISATEUR
                </a>

                 {% endif %} 
            </div>

            <div class="navbar-end">
                <div class="navbar-item">
                    <div class="buttons">
                        {% if app.user %}
                            <a class="button is-primary is-small" href="#">
                                <strong>{{ app.user.username }}</strong>
                            </a>
                            <a class="button is-light is-small" href="{{ path('app_logout')}}">
                                Déconnexion
                            </a>
                        {# {% else %} #}
                            {# <a class="button is-primary is-small" href="#">
                                <strong>Inscription</strong>
                            </a> #}
                            {# <a class="button is-light is-small" href="{{ path('app_login') }}">
                                Connexion
                            </a> #}
                        {% endif %}

                        {# {% if is_granted('ROLE_EDITOR') %}
                            <a class="button is-primary is-small" href="{{ path('admin_dashboard') }}">
                                <strong>Back Office</strong>
                            </a>
                        {% endif %} #}
                    </div>
                </div>

                {# <a class="navbar-item">
                    <form action="#">
                        <div class="field has-addons">
                            <div class="control is-expanded">
                                <input name="query" type="text" class="input is-small" placeholder="Recherche ..." required>
                            </div>
                            <div class="control">
                                <button type="submit" class="button is-primary is-small">
                                    <i class="fas fa-search"></i>
                                </button>
                            </div>
                        </div>
                    </form>
                </a> #}
            </div>
        </div>
    </nav>

    {# Bloc qui contiendra le contenu de chaque page #}
    {% block content %}{% endblock %}

{% endblock %}