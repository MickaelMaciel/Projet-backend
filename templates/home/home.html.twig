{% extends "_template.html.twig" %}

{% block title %}
    Accueil
{% endblock %}

{% block content %}
    <div class="section">
        <h1 class="title">Accueil</h1>

        {% if is_granted('ROLE_ADMIN') %}
            <a href="{#{{ path('test') }}#}">Test compte admin</a>
        {% endif %}

    </div>
{% endblock %}
